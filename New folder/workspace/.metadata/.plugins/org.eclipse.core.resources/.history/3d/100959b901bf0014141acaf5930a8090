package MonsterRelated;

import mapRelated.BasicMap;

public class Creature {
	
	protected int x,y;
	protected char [][] entityArray;
	
	protected void setEntityArray(char[][] entityArray){this.entityArray = entityArray;}
	
	protected boolean search(char name){
			boolean found = false;
			for (int row = x+2; row < BasicMap.widthByTiles; row++)
			{
				for (int column = 0; column < BasicMap.heightByTiles; column++)
				{
					if (entityArray[row][column] == name)
					{
						found = true;
						break;
					}
				}
			}
			
			return found;
		}
	
	public int[] getPosition(){
		int[] position = new int[2];
		position[0] = x;
		position[1] = y;
		return position;
	}


	
	

}
